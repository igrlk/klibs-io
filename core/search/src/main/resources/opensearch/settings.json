{
  "number_of_shards": 1,
  "number_of_replicas": 0,
  "analysis": {
    "tokenizer": { "code_tokenizer": { "type": "pattern", "pattern": "[^a-zA-Z0-9]+" } },
    "analyzer": { "code_split": { "type": "custom", "tokenizer": "code_tokenizer", "filter": ["lowercase"] } }
  }
}
